# 观己设计语言 v2（基于汇总页 v23 定稿）

> 版本：2026-08-08 · 来源：汇总页原型 v23（用户确认满意）
> 定位：作为整个 App 设计语言更新的基础规范（替代/演进 #53 排版规范与旧样式）

## 1. 设计哲学

**克制的层次感**——「一眼看到主角，细节有序退后」。三大原则：
1. **三级层次贯穿全页**：蓝（主）> 深灰（次主）> 浅灰（弱）——所有文字与控件按此分层，永不模糊
2. **呼吸与节奏**：间距成体系（大节奏 40 / 中 24 / chips 内 12），页面是连续体而非拼块
3. **轻悬浮**：控件带柔和阴影/光晕（不贴地），但克制不浮夸

## 2. 色彩体系（三级层次）

| 层次 | 色值 | 用途 |
|---|---|---|
| **主 · 活泼蓝** | `#007AFF`（--blue-live） | 大数字、主按钮、选中态、强调 |
| **次主 · 深灰** | `#3A3A3C`（--gray-strong） | 区块标题、正文、次级控件文字 |
| **弱 · 浅灰** | `#8E8E93`（--gray-weak） | 添加项、放弃、元信息 |

辅助色：
- 背景 `#F2F2F7`（--bg，主页一致）｜卡片/控件底白 `rgba(255,255,255,0.85~0.95)`
- chips 底 `#E5E5EA`（--bg-elev）｜选中底 `rgba(255,255,255,0.95)` + 蓝字
- 开关 on `#34C759`（--sage）
- 蓝色光晕 `rgba(0,122,255,0.35)`（大数字/主按钮投影）

## 3. 排版（延续 #53 阶梯，微调）

| 元素 | 字号 | 字重 | 字距 | 备注 |
|---|---|---|---|---|
| 大数字 | 40px（clamp 32-40） | 800 | -0.02em | tabular-nums · nowrap 单行 · **蓝光晕投影** 0 2px 14px rgba(0,122,255,0.35) |
| 区块标题 | 13px | 700 | 0.02em | 深灰 |
| chips | 13px | 600 | 0.02em | 深灰 |
| 主按钮 | 17px | 700 | 0.02em | 白字 |
| 次级按钮 | 13px | 700/600 | 0.02em | 深灰/浅灰 |

## 4. 间距体系（节奏）

| 关系 | 值 |
|---|---|
| 大数字下方（主角→内容） | **40px** |
| 板块之间（情绪→诱因） | **24px** |
| 内容→按钮区 | **40px** |
| 屏幕底部 padding | 44px |
| chips 之间 | 12px（阴影呼吸空间） |
| 区块标题→chips | 12px |

节奏口诀：**40 / 24 / 40**——对称连续，页面是一体。

## 5. 组件规范

### 按钮（三级）
| 层级 | 样式 |
|---|---|
| **主按钮（存为记录）** | 蓝底 `#007AFF` + 白字 17px/700 · 圆角 999 · 阴影 `0 8px 24px rgba(0,122,255,0.35)` |
| **次主（继续计时）** | 白底 85% + 深灰字 13px/700 · 阴影 `0 4px 14px rgba(0,0,0,0.10)` |
| **弱（放弃）** | 白底 40% + 浅灰字 13px/600 · 阴影 `0 2px 8px rgba(0,0,0,0.06)` |
- 统一：圆角 999、active scale(0.97)、focus-visible 轮廓

### chips（多选胶囊）
- 未选：`#E5E5EA` 底 + 深灰字 + **透明边框**（无可见线框）+ 阴影 `0 2px 6px rgba(0,0,0,0.08)`
- 选中：白底 95% + **蓝字** + 蓝调阴影 `0 2px 8px rgba(0,122,255,0.18)`
- 添加项：无底 + 浅灰虚线边框 + 浅灰字（弱层次）

### 大数字
- 蓝 `#007AFF` / 800 / -0.02em / tabular-nums / **nowrap 单行**（clamp 32-40px 适配小屏）
- 蓝光晕投影（不平凡、不黑）

### 开关
- 轨道 `#E5E5EA`，on `#34C759`，白色 knob（0.2s 过渡）

## 6. 阴影与深度（悬浮体系）

| 元素 | 阴影 |
|---|---|
| 大数字 | text-shadow 0 2px 14px rgba(0,122,255,0.35)（光晕） |
| 主按钮 | 0 8px 24px rgba(0,122,255,0.35)（蓝调悬浮） |
| 次主按钮 | 0 4px 14px rgba(0,0,0,0.10) |
| 弱按钮 | 0 2px 8px rgba(0,0,0,0.06) |
| chips | 0 2px 6px rgba(0,0,0,0.08)（选中 0 2px 8px 蓝调） |

原则：**层级越高阴影越重/越蓝**；阴影给控件「悬浮感」但克制。

## 7. 动效（延续现有语言）
- 控件按压：active scale(0.97)（即时反馈）
- 开关 knob：0.2s 过渡
- 视图切换：同容器瞬间切换（计时↔汇总）
- 尊重 prefers-reduced-motion

## 8. Do's and Don'ts

**✅ Do**
- 全页按三级层次配色（蓝 > 深灰 > 浅灰），不引入第四级
- 间距用 40/24/12 体系，保持节奏连续
- 主按钮用蓝底白字（与主色呼应）；大数字配蓝光晕
- chips 无可见边框（底色区分），阴影留呼吸
- 所有颜色走变量（--blue-live/--gray-strong/--gray-weak）
- 数字 nowrap 单行 + clamp 响应式

## 9. 液态玻璃 Material 分层（#72，实验性，可开关）

> 玻璃材质统一由 `html.liquid-glass` 类控制（我的页开关，localStorage `guanji_liquid_glass`），
> 默认开启；关闭即回退经典样式。全部走 CSS 变量，禁止硬编码。

### 9.1 Material 分层（透明度 = 层级）

| 层级 | 组件 | tint（浅/深） | 说明 |
|---|---|---|---|
| L0 无玻璃 | 计时全屏页/汇总页 | — | 沉浸蓝渐变，不参与玻璃体系 |
| L1 通透玻璃 | tab 栏 / 记录面板 / 日历弹层 | 0 / 0.05 | 最高通透，边缘结构承担玻璃感 |
| L2 遮罩玻璃 | 对话框 | 0.85 / 0.85 | 0.4 黑遮罩上需可读性 |
| L3 内容玻璃 | card / report-card / ask-answer | 0.60 / 0.08 | 半透明透出背景蓝晕，保持阅读 |
| L4 实色玻璃 | 按钮 / chips 选中 | 实色 accent | 原色保留 + 玻璃边缘（亮边/高光/光晕） |

### 9.2 玻璃边缘结构（共享）

- **渐变边框环**：`::after` + 环形 mask（content-box xor）——上亮 0.95 → 下暗 0.28，不碰主体
- **顶部受光 + 斜向光带**：`::before` radial + linear 叠加
- **阴影栈**：外侧发丝暗线 0.5px + 内侧白亮环 + 上缘 1px 高光 + 色相光晕
- **无磨砂**：backdrop-filter 仅 `saturate(2)`，禁 blur（#44 性能前科）

### 9.3 流动性（内容感知）

- tab 栏滚动收缩：任意 screen 滚动 >60px → `.scrolled`（58→50px + 玻璃变实 `--lg-tint-scroll`）
- 按压反馈：tab 栏 saturate 增强、按钮 scale
- 色散近似：大数字极轻 RGB 双色 text-shadow（0.18 alpha，克制防假）

### 9.4 降级

- `prefers-reduced-transparency`：玻璃组件回退实底 var(--card)
- `prefers-reduced-motion`：动画禁用
- 开关可随时关闭（我的 → 外观 → 液态玻璃（实验性））

**❌ Don't**
- 不用黑色作层次区分（黑字/黑底）——用深灰
- 不用白色文字体系在浅色背景上（不可读）
- 不引入卡片包裹（无边框白卡突兀）
- 不放大数字超 44px（过猛）、不留白超 44px（空旷）
- 不随意加边框线（灰线感）
- 不堆多层阴影（悬浮克制）

## 9. 迁移要点（旧规范 → v2）

| 旧（#53/v3.x） | v2 更新 |
|---|---|
| 主强调 --accent-deep #0062CC | **--blue-live #007AFF**（活泼蓝，用于主层次） |
| 按钮 btn-ghost 灰底 | 次主：**白底 85% + 深灰字**（悬浮感） |
| 卡片 + 阴影 | 控件独立悬浮（阴影体系），少卡片 |
| 间距 16/20 杂 | **40/24/12 节奏体系** |
| 数字无投影 | 大数字**蓝光晕投影** |
| chips 边框 var(--line) | **透明边框** + 底色区分 + 阴影 |

应用范围：记录面板、首页焦点、设置页等——按三级层次与间距体系逐步对齐（增量迁移，逐屏替换）。
