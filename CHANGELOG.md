# 观己 · 更新日志

> 「观己」是一个温和、非评判的习惯记录 App。数据仅存本地，AI 分析只接收聚合统计，不涉及单条记录。
> 更新日期：2026-08-06

---

## [2.5] AI 配置体验修复

### 修复
- 配置 AI 时软键盘弹出，底部 tab 栏不再被顶到键盘上方——键盘弹出时 tab 栏自动隐藏，收起后恢复
- AI 提供商切换不再串密钥：DeepSeek / OpenAI / 自定义的 API 密钥**分别保存**，切换时自动回显各自的配置（旧版已保存的密钥自动迁移，不丢失）

---

## [2.4] 大字体适配修复

### 修复
- 系统字体放大（textZoom）时，「次数趋势」切换胶囊的「30 天」不再断行上下排布——胶囊文字永不换行、不被压缩（标题自动让位截断）
- 「我的」页 API 密钥「保存」按钮不再被输入框挤压——输入框可收缩让位，按钮完整并排
- 记录面板的时间分段控件同样受益（文字不再断行）

---

## [2.3] 小组件点击统一 · 记录编辑 · 报告自动刷新

### 新增
- **编辑记录**：首页「最近记录」和日历明细新增「编辑」按钮，打开记录面板自动预填（时间/情绪/诱因/时长/看片/备注），保存后原地更新，不会产生重复记录
- **报告自动刷新**：修改或删除记录后，AI 报告自动重新生成，保证时效性与可信度——在分析页时防抖 1.5 秒自动刷新（顶部轻提示，不打断阅读）；在其他页面时切回分析页自动刷新；刷新失败保留旧报告并温和提示
- 小组件点击行为统一：数据看板 / 连续记录 / 今日卡片 / 本周节奏点击只打开 App 首页，不再弹出记录面板

### 优化
- 快速记录小组件：移除「打开记录面板」按钮，整卡即一键快速记录（语义更纯粹）
- 次级按钮样式全局改进：文字加深 + 加边框，不再「灰得像禁用」

### 修复
- 分析页「重新生成分析」按钮：改为醒目主按钮样式；生成期间禁用防重复点击，失败后自动恢复（此前失败后会永久变灰）
- 编辑超长时长记录不再丢失数据（滑块上限 60 分钟，超出的时长以显示文本为准）
- 报告指纹升级为内容摘要：编辑情绪/时长等内容也能正确触发报告刷新

---

## [2.2] 桌面小组件全家桶（5 个 2x2）

### 新增
- **一键快速记录**：桌面「＋」按钮直接记录一条「就现在」，无需打开 App；顶部显示今日记录状态
- **数据看板**：本周次数大数字 + 较上周环比 + 今日次数 + 连续天数
- **本周节奏**：周一至周日 7 根柱状图，今日高亮，标签随真实星期对齐
- **连续进度**：连续天数 + 进度条（迈向 7/30 天里程碑），温和鼓励文案
- **今日卡片**：今日是否已记录 + 温和状态文案
- 数据同步机制：App 内任何记录变化自动同步到全部小组件

---

## [2.1] 弹层动效升级

### 新增
- 弹层新增小横杠，**拖拽可关闭**（超过阈值下滑关闭，未达阈值回弹）
- 弹层出场/退场动画：进入上滑、退出下滑淡出、背景淡入淡出，统一动效语言

### 优化
- 记录面板改为**自适应高度**：两步切换时高度平滑过渡，不再有大片空白
- AI 设置卡标签间距统一（Base URL / 模型 / 密钥）

### 修复
- 记录提醒时间清空后自动恢复默认 21:00，显示与存储保持一致

---

## [2.0] AI 多提供商 · 记录体验完善（9 项）

### 新增
- **AI 多提供商**：默认 DeepSeek，可选 OpenAI 或自定义（Base URL + 模型 + API 密钥），支持连接测试与分类报错
- **情绪/诱因自定义**：内置选项外可自主添加，本地持久化
- 分析页追问回复排版优化：加粗/列表/分段渲染，与报告卡片风格统一

### 优化
- 首页 14 天/30 天切换、记录面板时间分段：滑块跟踪动效
- 日历弹层点击空白处即可关闭
- 记录面板两步切换高度统一，不再上下跳动

### 修复
- 按钮点击后残留灰色边框（移除触屏焦点框，保留键盘可访问性样式）
- 补记日期时间为空/非法时显示「NaN」（改为回退当前时间 + 保存校验拦截）
- 旧版 API 密钥自动迁移到新配置

---

## [1.9] 修复

### 修复
- 时段分布环图周长公式错误（少乘 2），导致有数据的时段渲染成断裂短弧、环上出现大片空白弧位——修复后环完整连续

---

## [1.8] 环图多彩配色

### 优化
- 时段分布环图升级为多彩时间色（黄/橙/绿/青/蓝，深色模式自动提亮），中心数字与环段解耦

---

## [1.7] 问候语优化

### 优化
- 问候语拆层：标题固定一行时段问候（永不截断），AI 提醒句移到下方小字完整显示

---

## [1.6] 小组件 · 提醒 · 趋势（6 项）

### 新增
- **桌面小组件（2x2）**：点击直达记录面板
- **每日记录提醒**：可设置温和提醒时间（默认关闭）
- 趋势图 14 天/30 天切换 + 月度汇总
- 情绪-频率关联分析（AI 报告新增情绪观察）
- 温和正向反馈：连续记录 7/30 天里程碑（可在设置中关闭）

### 优化
- 问候语长度三保险（提示词限长 + 截断 + 单行省略）

### 修复
- 补记按钮重复绑定导致的行为异常

---

## [1.5] 修复

### 修复
- 日历按钮被长问候语挤压变形

---

## [1.4] 日历视图 · 深色模式 · 自动分析

### 新增
- **历史记录日历视图**：月历角标 + 选天明细 + 补记直达
- **分析页自动生成**：检测到新数据自动生成报告（指纹防重复 + 失败自动重试 3 次）
- **深色模式**：跟随系统 + 手动覆盖，防闪烁，图表颜色同步适配

---

## [1.3] 全面屏适配

### 优化
- 适配安卓全面屏（安全区适配、沉浸式状态栏）
- 底部 Tab 滑块圆角修复

---

## [1.2] 数据可视化升级

### 新增
- 时段分布改为「一天 100%」五段分段环图（深浅渐变 + 色点图例 + 动画）
- Tab 栏滑块跟踪样式（弹性过渡）

---

## [1.1] 首轮体验优化

### 新增
- 问候语动态化：按时段问候，并接入 AI 健康提醒（组合成一句话）
- Tab 栏美化

### 修复
- 两个体验细节 bug

---

## [1.0] 首个版本

- 记录面板：就现在 / 补记（日期时间、情绪、诱因、时长、看片、备注）
- 首页：今日概览、时段分布环图、趋势图、最近记录（可删除）
- AI 分析：聚合统计特征（频率/时段/情绪/诱因/连续天数）生成温和非评判报告，支持追问
- 数据仅存本地（持久化），可导出 CSV、恢复演示数据
- 温和的隐私设计：AI 只接收聚合数据，不涉及单条记录
